{
  "dependencies": {
    "body-parser": "^1.20.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.7"
  },
  "scripts": {
    "dev": "nodemon server.js",
    "build:ui": "rm -rf dist && cd ../frontend/ && npm install && npm run build && cp -r dist ../backend",
    "fullstart": "npm run build:ui && npm start"
  }
}
